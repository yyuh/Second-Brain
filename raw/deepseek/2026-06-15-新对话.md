# 新对话
> 来源: DeepSeek DOM 提取
> 提取时间: 2026/6/16 06:11:38
> 会话 ID: /as/854ff1d8-d3e8-4148-8d72-0504791a497c
## 对话内容
**DeepSeek**:

已阅读 9 个网页在 n8n 中把多个角色融合进一个 Agent，目前最成熟的方案是建立“主控Agent + 工具Agent”的层级架构-1-7-9。核心逻辑是：一个主控Agent负责理解你的需求并当"调度员"，把具体任务派发给充当工具的各类专项Agent去执行-5。⚙️ 方案一：将其他Agent转化为“工具”（官方推荐）这是最符合 n8n 设计理念的方式，非常灵活-9。架构原理：配置一个主控Agent，然后在它的工具列表里添加 AI Agent Tool 节点。每个 Tool 节点内部可独立配置另一个 Agent（可赋予不同角色、提示词甚至模型）-9。分工协作：主控 Agent 负责意图识别与分发，专项 Agent 各司其职（如代码编写、文案生成），最后将结果汇总返回-1-5。适合场景：逻辑复杂的场景，比如先让“研究员Agent”上网搜资料，再由“作家Agent”整理成报告-5。🔄 方案二：利用“圆桌会议”模式（单一模型模拟）如果你不想消耗大量 Token 或配置复杂流程，可以安装 n8n-nodes-roundtable 社区节点-3。架构原理：它并不真的启动多个 Agent，而是连接同一个 LLM，通过预置的 Host（主持人） 和多个不同 Participant（参与者） 的系统提示词，模拟出多人开会的效果-3。适合场景：头脑风暴、多角度观点生成、内容创作，速度快且成本低-3。🚀 手把手：如何配置“主从架构”工作流参考如下步骤，在 n8n 画布上搭建一个多角色系统：搭建主控层 (Orchestrator)：拖入第一个 AI Agent（主控），配置你的主模型（如 GPT-4）。在它的 Tools 字段中，准备添加下面的执行者-1-9。创建执行者 (Workers)：拖入 AI Agent Tool 节点，将其连接到主控 Agent。这个节点负责扮演具体的“角色”-9。功能描述：极其关键。用一句话告诉主控什么时候该用你。例如“当你需要把中文翻译成英文时，调用此智能体”-9。System Message：设定这个角色的具体人设和规则（例如：“你是一位专业的技术文档翻译官”）。连接与扩展：按需复制多个 AI Agent Tool 节点（如“Python 代码专家”、“小红书文案专家”），全部连接到主控 Agent。n8n 会自动识别这些就是可用的“工具”-9。💡 避坑小贴士不要“放养”：虽然可以让 Agent 完全自主，但官方社区建议工作流最好是确定性的，即由 n8n 的逻辑节点来控制调用哪个 Agent，这比让 AI 自己做决定要稳定、省钱得多-7。描述要清晰：在 AI Agent Tool 节点的“功能描述”里一定要写清楚用途，否则主控 Agent 会不知道什么时候该用它-9。如果你能具体说一下你想融合的是哪几类角色（比如：前端程序员+UI设计师），我可以帮你写具体的提示词示例～9 个网页

---

**DeepSeek**:

在 n8n 中把多个角色融合进一个 Agent，目前最成熟的方案是建立“主控Agent + 工具Agent”的层级架构-1-7-9。核心逻辑是：一个主控Agent负责理解你的需求并当"调度员"，把具体任务派发给充当工具的各类专项Agent去执行-5。⚙️ 方案一：将其他Agent转化为“工具”（官方推荐）这是最符合 n8n 设计理念的方式，非常灵活-9。架构原理：配置一个主控Agent，然后在它的工具列表里添加 AI Agent Tool 节点。每个 Tool 节点内部可独立配置另一个 Agent（可赋予不同角色、提示词甚至模型）-9。分工协作：主控 Agent 负责意图识别与分发，专项 Agent 各司其职（如代码编写、文案生成），最后将结果汇总返回-1-5。适合场景：逻辑复杂的场景，比如先让“研究员Agent”上网搜资料，再由“作家Agent”整理成报告-5。🔄 方案二：利用“圆桌会议”模式（单一模型模拟）如果你不想消耗大量 Token 或配置复杂流程，可以安装 n8n-nodes-roundtable 社区节点-3。架构原理：它并不真的启动多个 Agent，而是连接同一个 LLM，通过预置的 Host（主持人） 和多个不同 Participant（参与者） 的系统提示词，模拟出多人开会的效果-3。适合场景：头脑风暴、多角度观点生成、内容创作，速度快且成本低-3。🚀 手把手：如何配置“主从架构”工作流参考如下步骤，在 n8n 画布上搭建一个多角色系统：搭建主控层 (Orchestrator)：拖入第一个 AI Agent（主控），配置你的主模型（如 GPT-4）。在它的 Tools 字段中，准备添加下面的执行者-1-9。创建执行者 (Workers)：拖入 AI Agent Tool 节点，将其连接到主控 Agent。这个节点负责扮演具体的“角色”-9。功能描述：极其关键。用一句话告诉主控什么时候该用你。例如“当你需要把中文翻译成英文时，调用此智能体”-9。System Message：设定这个角色的具体人设和规则（例如：“你是一位专业的技术文档翻译官”）。连接与扩展：按需复制多个 AI Agent Tool 节点（如“Python 代码专家”、“小红书文案专家”），全部连接到主控 Agent。n8n 会自动识别这些就是可用的“工具”-9。💡 避坑小贴士不要“放养”：虽然可以让 Agent 完全自主，但官方社区建议工作流最好是确定性的，即由 n8n 的逻辑节点来控制调用哪个 Agent，这比让 AI 自己做决定要稳定、省钱得多-7。描述要清晰：在 AI Agent Tool 节点的“功能描述”里一定要写清楚用途，否则主控 Agent 会不知道什么时候该用它-9。如果你能具体说一下你想融合的是哪几类角色（比如：前端程序员+UI设计师），我可以帮你写具体的提示词示例～

---

**DeepSeek**:

在 n8n 中把多个角色融合进一个 Agent，目前最成熟的方案是建立“主控Agent + 工具Agent”的层级架构-1-7-9。

---

**DeepSeek**:

核心逻辑是：一个主控Agent负责理解你的需求并当"调度员"，把具体任务派发给充当工具的各类专项Agent去执行-5。

---

**DeepSeek**:

这是最符合 n8n 设计理念的方式，非常灵活-9。

---

**DeepSeek**:

架构原理：配置一个主控Agent，然后在它的工具列表里添加 AI Agent Tool 节点。每个 Tool 节点内部可独立配置另一个 Agent（可赋予不同角色、提示词甚至模型）-9。

---

**DeepSeek**:

分工协作：主控 Agent 负责意图识别与分发，专项 Agent 各司其职（如代码编写、文案生成），最后将结果汇总返回-1-5。

---

**DeepSeek**:

适合场景：逻辑复杂的场景，比如先让“研究员Agent”上网搜资料，再由“作家Agent”整理成报告-5。

---

**DeepSeek**:

如果你不想消耗大量 Token 或配置复杂流程，可以安装 n8n-nodes-roundtable 社区节点-3。

---

**DeepSeek**:

架构原理：它并不真的启动多个 Agent，而是连接同一个 LLM，通过预置的 Host（主持人） 和多个不同 Participant（参与者） 的系统提示词，模拟出多人开会的效果-3。

---

**DeepSeek**:

适合场景：头脑风暴、多角度观点生成、内容创作，速度快且成本低-3。

---

**DeepSeek**:

参考如下步骤，在 n8n 画布上搭建一个多角色系统：

---

**DeepSeek**:

搭建主控层 (Orchestrator)：拖入第一个 AI Agent（主控），配置你的主模型（如 GPT-4）。在它的 Tools 字段中，准备添加下面的执行者-1-9。

---

**DeepSeek**:

创建执行者 (Workers)：拖入 AI Agent Tool 节点，将其连接到主控 Agent。这个节点负责扮演具体的“角色”-9。

---

**DeepSeek**:

功能描述：极其关键。用一句话告诉主控什么时候该用你。例如“当你需要把中文翻译成英文时，调用此智能体”-9。

---

**DeepSeek**:

System Message：设定这个角色的具体人设和规则（例如：“你是一位专业的技术文档翻译官”）。

---

**DeepSeek**:

连接与扩展：按需复制多个 AI Agent Tool 节点（如“Python 代码专家”、“小红书文案专家”），全部连接到主控 Agent。n8n 会自动识别这些就是可用的“工具”-9。

---

**DeepSeek**:

不要“放养”：虽然可以让 Agent 完全自主，但官方社区建议工作流最好是确定性的，即由 n8n 的逻辑节点来控制调用哪个 Agent，这比让 AI 自己做决定要稳定、省钱得多-7。

---

**DeepSeek**:

描述要清晰：在 AI Agent Tool 节点的“功能描述”里一定要写清楚用途，否则主控 Agent 会不知道什么时候该用它-9。

---

**DeepSeek**:

如果你能具体说一下你想融合的是哪几类角色（比如：前端程序员+UI设计师），我可以帮你写具体的提示词示例～

---

**DeepSeek**:

我把他们说的话都以文字的形式放到skill里，可以非常轻量化的实现吗

---

**DeepSeek**:

完全可以，而且这正是 n8n 里最轻量、最实用的做法。你不需要搭建多个 Agent，只要把每个人的“说话风格/知识”写进提示词就行。核心思路：一个 AI Agent 节点 + 系统提示词里定义好 N 种角色具体做法：拖一个 AI Agent 节点（用 Tools Agent 或 Conversational Agent 都可以）在 System Prompt 里这样写：text复制下载你是一个团队，包含以下成员：

【程序员老王】性格直接，说话爱加“咳咳”，代码给完整可运行版本
【设计师Lisa】语气温柔，关注用户体验和美观，建议会画ASCII示意图
【老板张总】喜欢画饼，强调商业价值，每句话结尾加“听懂掌声”

用户提问时，你需要判断该让谁回答，并用ta的语气、风格、知识来回复。

如果问题需要多人协作，可以依次用【角色名】开头分别输出观点。把你要融合的对话记录整理成“案例”，也放进提示词（Few-shot）优化技巧：用变量 {{ $json.user_input }} 接收用户问题如果某个角色知识太多（比如500页文档），不要全塞提示词，改用 Vector Store + 按需检索想让角色更稳定，可以在提示词里加“强制格式”：回复格式：【当前角色】+ 内容一个角色占用多少空间？大概50-200字描述。如果你有5个人，提示词也就1000字左右，对性能和成本几乎没有影响。这就是最轻量的“融合”——本质上是一个会“模仿多人”的AI，而不是真的跑多个Agent。

---

**DeepSeek**:

完全可以，而且这正是 n8n 里最轻量、最实用的做法。你不需要搭建多个 Agent，只要把每个人的“说话风格/知识”写进提示词就行。

---

**DeepSeek**:

核心思路：一个 AI Agent 节点 + 系统提示词里定义好 N 种角色

---

**DeepSeek**:

具体做法：

---

**DeepSeek**:

拖一个 AI Agent 节点（用 Tools Agent 或 Conversational Agent 都可以）

---

**DeepSeek**:

在 System Prompt 里这样写：

---

**DeepSeek**:

把你要融合的对话记录整理成“案例”，也放进提示词（Few-shot）

---

**DeepSeek**:

优化技巧：

---

**DeepSeek**:

用变量 {{ $json.user_input }} 接收用户问题

---

**DeepSeek**:

如果某个角色知识太多（比如500页文档），不要全塞提示词，改用 Vector Store + 按需检索

---

**DeepSeek**:

想让角色更稳定，可以在提示词里加“强制格式”：回复格式：【当前角色】+ 内容

---

**DeepSeek**:

一个角色占用多少空间？大概50-200字描述。如果你有5个人，提示词也就1000字左右，对性能和成本几乎没有影响。

---

**DeepSeek**:

这就是最轻量的“融合”——本质上是一个会“模仿多人”的AI，而不是真的跑多个Agent。

---
