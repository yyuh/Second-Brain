# BMS测试方案
> 来源: DeepSeek DOM 提取
> 提取时间: 2026/6/16 06:11:56
> 会话 ID: /as/bc41fa37-c9d0-4e27-944a-bcc7d2c045df
## 对话内容
**DeepSeek**:

B- 和 P- 之间电压是0v就是两个管子都开了，要是0.7v那就是开了一个

---

**DeepSeek**:

你总结的这个规律非常接近真相，但需要做一个关键修正，否则容易在测试中误判。一、先给你准确结论B- 和 P- 之间电压 ≈ 0V → 两个管子（充放电MOS）都导通（正常放电或充电状态）B- 和 P- 之间电压 ≈ 0.7V → 只有一个管子导通，另一个关断或体二极管在承压你说的“开了一个”✅ 基本正确，但不一定是“开”，更准确说是：一个导通，另一个的体二极管被正向偏置，产生了0.7V压降。二、为什么是0.7V？（这是关键）0.7V 是硅二极管的正向导通压降。每个MOS管内部都寄生一个体二极管（源极→漏极方向）。两个MOS管的结构（串联在 B- 和 P- 之间）text复制下载B- --[放电MOS管]--[充电MOS管]-- P-
         ↓              ↓
      体二极管       体二极管
      （方向：S→D）  （方向：S→D）不同状态下 B- 到 P- 的电压状态放电MOS充电MOSB- 到 P- 电压说明正常放电导通导通≈ 0V（几十mV）两个管子都开，压降很小正常充电导通导通≈ 0V（几十mV）同上过放保护关断导通≈ 0.7V电流通过放电MOS的体二极管过充保护导通关断≈ -0.7V（反向）充电时才会出现短路保护关断关断等于电池电压完全切断你观察到的 0.7V 对应的就是：放电MOS关断（体二极管承压）+ 充电MOS导通三、为什么0.7V时“只开了一个”？你说的“开了一个”在物理上其实是：一个MOS管的沟道导通（正常开）另一个MOS管关断，但它的体二极管被正向导通（不是沟道开，是二极管开）从电流能流过的角度，你判断“开了一个”是对的。从MOS管开关状态角度，更严谨的说法是：一个导通 + 一个体二极管导通四、反过来，如果两个管子都关断B- 和 P- 之间电压 不等于 0.7V，而是：如果没有电流路径 → 电压 = 电池电压（悬空时）如果有外部电压强行加进去 → 电压由外部决定0.7V 是一个非常特殊的中间状态，只在 其中一个管子关断、电流通过体二极管 时出现。五、这和你之前的问题串联起来了之前你问：为什么在 P- 和 B- 之间施加 0.7V 会使 MOS 管打开？现在可以完整回答了：你施加 0.7V 时，这个电压刚好让某一个管子的体二极管正向导通体二极管导通后，电流流过采样电阻，产生压降BMS 芯片检测到这个压降，认为有电流在流动根据BMS的逻辑（充电或放电方向），它可能会主动导通另一个管子最终呈现的效果就是：MOS管“被打开了”所以不是 0.7V 直接打开了MOS管，而是 0.7V 触发了BMS的逻辑，让BMS去打开了管子。六、你可以这样记住B- 到 P- 电压含义判断≈ 0V两个MOS都导通✅ 正常工作≈ 0.7V（正）放电MOS关断（体二极管导通），充电MOS导通⚠️ 过放保护状态≈ 0.7V（负）充电MOS关断（体二极管导通），放电MOS导通⚠️ 过充保护状态≈ 电池电压两个MOS都关断⚠️ 短路或深度保护一句话总结你的发现：B-和P-之间0V = 两个管子全开；0.7V = 一个管子的沟道开 + 另一个管子的体二极管开（保护状态）这是一个很敏锐的观察，说明你已经在深入理解BMS的内部工作了。

---

**DeepSeek**:

你总结的这个规律非常接近真相，但需要做一个关键修正，否则容易在测试中误判。

---

**DeepSeek**:

B- 和 P- 之间电压 ≈ 0V → 两个管子（充放电MOS）都导通（正常放电或充电状态）

---

**DeepSeek**:

B- 和 P- 之间电压 ≈ 0.7V → 只有一个管子导通，另一个关断或体二极管在承压

---

**DeepSeek**:

你说的“开了一个”✅ 基本正确，但不一定是“开”，更准确说是：一个导通，另一个的体二极管被正向偏置，产生了0.7V压降。

---

**DeepSeek**:

0.7V 是硅二极管的正向导通压降。

---

**DeepSeek**:

每个MOS管内部都寄生一个体二极管（源极→漏极方向）。

---

**DeepSeek**:

你观察到的 0.7V 对应的就是：放电MOS关断（体二极管承压）+ 充电MOS导通

---

**DeepSeek**:

你说的“开了一个”在物理上其实是：

---

**DeepSeek**:

一个MOS管的沟道导通（正常开）

---

**DeepSeek**:

另一个MOS管关断，但它的体二极管被正向导通（不是沟道开，是二极管开）

---

**DeepSeek**:

从电流能流过的角度，你判断“开了一个”是对的。从MOS管开关状态角度，更严谨的说法是：

---

**DeepSeek**:

一个导通 + 一个体二极管导通

---

**DeepSeek**:

B- 和 P- 之间电压 不等于 0.7V，而是：

---

**DeepSeek**:

如果没有电流路径 → 电压 = 电池电压（悬空时）

---

**DeepSeek**:

如果有外部电压强行加进去 → 电压由外部决定

---

**DeepSeek**:

0.7V 是一个非常特殊的中间状态，只在 其中一个管子关断、电流通过体二极管 时出现。

---

**DeepSeek**:

之前你问：

---

**DeepSeek**:

为什么在 P- 和 B- 之间施加 0.7V 会使 MOS 管打开？

---

**DeepSeek**:

现在可以完整回答了：

---

**DeepSeek**:

你施加 0.7V 时，这个电压刚好让某一个管子的体二极管正向导通

---

**DeepSeek**:

体二极管导通后，电流流过采样电阻，产生压降

---

**DeepSeek**:

BMS 芯片检测到这个压降，认为有电流在流动

---

**DeepSeek**:

根据BMS的逻辑（充电或放电方向），它可能会主动导通另一个管子

---

**DeepSeek**:

最终呈现的效果就是：MOS管“被打开了”

---

**DeepSeek**:

所以不是 0.7V 直接打开了MOS管，而是 0.7V 触发了BMS的逻辑，让BMS去打开了管子。

---

**DeepSeek**:

一句话总结你的发现：

---

**DeepSeek**:

B-和P-之间0V = 两个管子全开；0.7V = 一个管子的沟道开 + 另一个管子的体二极管开（保护状态）

---

**DeepSeek**:

这是一个很敏锐的观察，说明你已经在深入理解BMS的内部工作了。

---

**DeepSeek**:

，用示波器抓取负载端电压变化曲线，这个我应该量那两点

---

**DeepSeek**:

针对你之前一直在调试的 B+、B-、P- 系统，并且是为了验证 预充/预放电过程是否平滑、无火花，最标准、最直接的测量点是：明确答案：量 B+ 和 P- 这两点示波器探头（正极）接：B+示波器地线（负极）接：P-为什么要量这两点？你要抓的是 “负载端电压”。在由 B+、B-、P- 组成的系统中：负载的正极：直接连在 B+ 上（通常 B+ 与 P+ 内部直通，或直接接到负载正极）。负载的负极：直接连在 P- 上。所以，B+ 和 P- 之间的电压，就是负载实际承受的电压。上电瞬间：负载电容没电，B+ 和 P- 之间电压为 0V。预充过程中：电压从 0V 开始平滑上升。预充完成：电压接近 B+ 和 B- 之间的总电压。如果你量 B+ 和 B-，你只会看到一条平稳的直线（电池总电压），完全看不到预充的动态过程。**你的示波器设置参考通道设置：探头比例：1X（如果无衰减）或 10X。耦合：DC 耦合。时基：建议 100ms/div 或 200ms/div。预充过程通常持续 50ms ~ 500ms，这个时基能看清完整的上升曲线。垂直档位：根据总电压设置，比如总电压 48V，设为 10V/div，让波形占屏幕中间 4-5 格。触发设置：类型：边沿触发。斜率：上升沿。触发电平：设为 0.5V ~ 1V。触发模式：单次 (Single) 或 普通 (Normal)。操作：先按 Single（单次）让示波器等待。给 BMS 上电（或闭合预充开关）。示波器会自动抓到并定格波形。合格 vs 不合格波形（看图说话）✅ 合格（平滑完成）：波形从 0V 开始，呈圆滑的对数曲线上升，最终稳定在总电压附近。没有台阶、没有毛刺、没有瞬间跳变。❌ 不合格（有冲击/火花）：波形底部有个瞬间的尖峰：上电瞬间就跳到高电压再降下来 → 有火花。波形中间有垂直的台阶：继电器或开关抖动。波形一开始就是直线高电压：预充电路没工作。一个特殊情况（你的板子有 P- 但没有 P+）之所以反复强调量 B+ 到 P-，是因为有些板子没有引出 P+ 端子。如果有 P+ 端子：量 P+ 到 P- 的结果和 B+ 到 P- 是一样的。如果只有 B+、B-、P-：那就必须量 B+ 到 P-，这是唯一正确的位置。**总结操作把示波器探头接在 B+（正极）和 P-（负极）上，抓上电瞬间的电压上升曲线。

---

**DeepSeek**:

针对你之前一直在调试的 B+、B-、P- 系统，并且是为了验证 预充/预放电过程是否平滑、无火花，最标准、最直接的测量点是：

---

**DeepSeek**:

示波器探头（正极）接：B+

---

**DeepSeek**:

示波器地线（负极）接：P-

---

**DeepSeek**:

你要抓的是 “负载端电压”。

---

**DeepSeek**:

在由 B+、B-、P- 组成的系统中：

---

**DeepSeek**:

负载的正极：直接连在 B+ 上（通常 B+ 与 P+ 内部直通，或直接接到负载正极）。

---

**DeepSeek**:

负载的负极：直接连在 P- 上。

---

**DeepSeek**:

所以，B+ 和 P- 之间的电压，就是负载实际承受的电压。

---

**DeepSeek**:

上电瞬间：负载电容没电，B+ 和 P- 之间电压为 0V。

---

**DeepSeek**:

预充过程中：电压从 0V 开始平滑上升。

---

**DeepSeek**:

预充完成：电压接近 B+ 和 B- 之间的总电压。

---

**DeepSeek**:

如果你量 B+ 和 B-，你只会看到一条平稳的直线（电池总电压），完全看不到预充的动态过程。**

---

**DeepSeek**:

通道设置：

---

**DeepSeek**:

探头比例：1X（如果无衰减）或 10X。

---

**DeepSeek**:

耦合：DC 耦合。

---

**DeepSeek**:

建议 100ms/div 或 200ms/div。预充过程通常持续 50ms ~ 500ms，这个时基能看清完整的上升曲线。

---

**DeepSeek**:

垂直档位：

---

**DeepSeek**:

根据总电压设置，比如总电压 48V，设为 10V/div，让波形占屏幕中间 4-5 格。

---

**DeepSeek**:

触发设置：

---

**DeepSeek**:

类型：边沿触发。

---

**DeepSeek**:

斜率：上升沿。

---

**DeepSeek**:

触发电平：设为 0.5V ~ 1V。

---

**DeepSeek**:

触发模式：单次 (Single) 或 普通 (Normal)。

---

**DeepSeek**:

先按 Single（单次）让示波器等待。

---

**DeepSeek**:

给 BMS 上电（或闭合预充开关）。

---

**DeepSeek**:

示波器会自动抓到并定格波形。

---

**DeepSeek**:

✅ 合格（平滑完成）：

---

**DeepSeek**:

波形从 0V 开始，呈圆滑的对数曲线上升，最终稳定在总电压附近。

---

**DeepSeek**:

没有台阶、没有毛刺、没有瞬间跳变。

---

**DeepSeek**:

❌ 不合格（有冲击/火花）：

---

**DeepSeek**:

波形底部有个瞬间的尖峰：上电瞬间就跳到高电压再降下来 → 有火花。

---

**DeepSeek**:

波形中间有垂直的台阶：继电器或开关抖动。

---

**DeepSeek**:

波形一开始就是直线高电压：预充电路没工作。

---

**DeepSeek**:

之所以反复强调量 B+ 到 P-，是因为有些板子没有引出 P+ 端子。

---

**DeepSeek**:

如果有 P+ 端子：量 P+ 到 P- 的结果和 B+ 到 P- 是一样的。

---

**DeepSeek**:

如果只有 B+、B-、P-：那就必须量 B+ 到 P-，这是唯一正确的位置。**

---

**DeepSeek**:

把示波器探头接在 B+（正极）和 P-（负极）上，抓上电瞬间的电压上升曲线。

---
